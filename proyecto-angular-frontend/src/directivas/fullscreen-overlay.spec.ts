import { FullscreenOverlay } from './fullscreen-overlay';

describe('FullscreenOverlay', () => {
  it('should create an instance', () => {
    const directive = new FullscreenOverlay();
    expect(directive).toBeTruthy();
  });
});
