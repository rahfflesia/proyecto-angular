import { Component } from '@angular/core';
import { Cabecera } from '../cabecera/cabecera';

@Component({
  selector: 'notas',
  imports: [Cabecera],
  templateUrl: './notas.html',
  styleUrl: './notas.css',
})
export class Notas {}
